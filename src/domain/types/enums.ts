export enum IssueType {
  BUG = "bug",
  STORY = "story",
  TASK = "task",
}
export enum StatusType {
  BACKLOG = 0,
  IN_PROGRESS = 1,
  DONE = 2,
}
export enum ProjectPermission {
  MEMBER = 0,
  MODERATOR = 1,
  ADMINISTRATOR = 2,
}
export enum issuePriority {
  LOW = 0,  
  MEDIUM = 1,
  HIGH = 2,
}